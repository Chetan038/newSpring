<p>login works!</p>

<form (ngSubmit)="onSubmit()">
    
   
    <input type="text" placeholder="email" name="username" [(ngModel)]="credentials.email" required><br>
    <input type="text" placeholder="password" name="password" [(ngModel)]="credentials.password" required><br>
    <button (click)="onSubmit()" type="submit">Login</button>

</form>

NOW do the style for this alsoo and maintain the theme
